# run sudo rpi-eeprom-update to check version
# run sudo rpi-eeprom-update -a to update if needed
# run sudo rpi-eeprom-update --edit to edit file

# add this line to end of file to fix fan behavior of cm5 on shutdown
POWER_OFF_ON_HALT=0

# possible bash script usage? 
#!/bin/bash
echo "POWER_OFF_ON_HALT=0" >> rpi-eeprom-config
